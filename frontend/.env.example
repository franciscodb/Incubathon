# =====================================================================
# Frontend — CumplIA (arquitectura 3 capas)
# El frontend habla SOLO con el backend FastAPI. Copia este archivo a `.env`.
# El modo demo/producción lo decide el BACKEND (USE_MOCK en backend/.env),
# NO el frontend. Aquí ya no van llaves de Supabase.
# =====================================================================

# URL base del backend FastAPI (local o Railway).
VITE_API_URL=http://localhost:8000
